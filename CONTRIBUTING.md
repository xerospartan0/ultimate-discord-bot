# Contributing

Thanks for helping improve the Ultimate Bot!

- Fork the repo and open a pull request.
- Write tests for new features under `tests/` using `pytest`.
- Run `flake8` and `pytest` locally before submitting.
- Keep secrets out of commits. Use `.env` and `.env.example`.
